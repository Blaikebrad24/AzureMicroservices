# Local development overrides
# This file is automatically loaded by docker compose alongside docker-compose.yml
# It exposes additional ports and mounts volumes for local debugging

services:
  blob-service:
    ports:
      - "8081:8080"

  reports-service:
    ports:
      - "8082:8080"

  data-service:
    ports:
      - "8083:8080"

  frontend:
    ports:
      - "3000:3000"
